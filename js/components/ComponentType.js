define ([], function () {
    var ComponentType = {
        Transform: "transform",
        Renderer: "renderer",
        Inputable: "inputable",
        GameButton: "gameButton"
    };

    return ComponentType;
});